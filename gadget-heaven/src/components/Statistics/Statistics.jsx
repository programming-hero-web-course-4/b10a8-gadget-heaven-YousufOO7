import PropTypes from 'prop-types';

const Statistics = props => {
    return (
        <div>
            <h2 className="text-4xl">Statistics page</h2>
        </div>
    );
};

Statistics.propTypes = {
    
};

export default Statistics;