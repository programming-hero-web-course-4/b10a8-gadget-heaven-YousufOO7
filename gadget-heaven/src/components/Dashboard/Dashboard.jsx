import PropTypes from 'prop-types';

const Dashboard = props => {
    return (
        <div>
            <h2 className="text-4xl">Dashboard page</h2>
        </div>
    );
};

Dashboard.propTypes = {
    
};

export default Dashboard;